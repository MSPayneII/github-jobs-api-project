import React from "react";

// This component displays an error message if an error occurs during api data retrieval
const Error = () => {
  return (
    <div className="prompt container">
      <h1 className="prompt-title">Error...</h1>
    </div>
  );
};

export default Error;
