import React from "react";

// A prompt that lets the user know when data is being retrieved and loaded to the application

const Loading = () => {
  return (
    <div className="prompt container">
      <h1 className="prompt-title">Loading...</h1>
    </div>
  );
};

export default Loading;
